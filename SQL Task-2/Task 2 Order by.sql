-- SELECT *  FROM customers ORDER BY customer_name ASC;


 --select order_id, product_id, total_price from order_items order by total_price desc;

--select product_name, price, category from products order by price asc , category desc;

-- select order_id, customer_id, order_date from orders order by order_date desc;

-- select delivery_city, count (order_id) as total_orders from orders
-- group by delivery_city order by  delivery_city asc;
